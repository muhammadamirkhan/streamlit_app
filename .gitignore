# Secrets — never commit
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local test / scratch files
_test_*.xlsx
mv_test*.xlsx
~$*.xlsx

# base_version.json is COMMITTED on purpose (committed once = the permanent client baseline that
# survives Cloud redeploys/reboots/sleep; it is loaded on launch). The working session state and
# comments stay local/ephemeral.
app_state.json
unit_comments.json

# OS
.DS_Store
Thumbs.db
MV-Pricing.pbix
MV-Pricing.pbix.zip
muraba_app.py
Launch Muraba App.bat
