# Secrets — never commit
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local test / scratch files
_test_*.xlsx
mv_test*.xlsx
~$*.xlsx

# base_version.json AND muraba_comments.json are COMMITTED on purpose (the permanent client
# baseline + the persisted comments; both survive Cloud redeploys/reboots/sleep and load on launch).
# Only the live working session stays local/ephemeral.
app_state.json
unit_comments.json

# OS
.DS_Store
Thumbs.db
MV-Pricing.pbix
MV-Pricing.pbix.zip
muraba_app.py
Launch Muraba App.bat
